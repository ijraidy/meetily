/**
 * Analytics consent switch.
 *
 * Telemetry is permanently disabled in this build, so there is nothing for the
 * user to opt in or out of. The component is kept so existing imports still
 * resolve, but it renders nothing.
 */
export default function AnalyticsConsentSwitch() {
  return null;
}
